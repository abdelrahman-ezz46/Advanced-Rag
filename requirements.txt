# Web Framework
# FastAPI now powers the backend; the React/Next frontend lives in ./frontend
# and talks to this server over JSON. Streamlit is kept as a legacy/fallback UI.
fastapi
uvicorn[standard]
python-multipart
streamlit

# Local LLM Integration
# Drives chunking, contextual retrieval, query rewriting (multi-turn),
# and answer generation — all locally via Ollama.
ollama

# Vector Database & Retrieval
# ChromaDB stores per-chunk metadata (source, modality, indexed_at,
# indexed_ts) and is queried with $and / $in / $gte / $lte for the
# UI metadata filters.
chromadb
rank_bm25

# Cross-Encoder Reranker (optional — enable with RAGConnector(use_reranker=True))
sentence-transformers

# Web URL Ingestion
# httpx fetches pages; trafilatura strips boilerplate and returns clean Markdown.
httpx
trafilatura

# Document Parsing (PDFs, Word, etc.)
docling

# Video & Audio Processing
ffmpeg-python
openai-whisper

# Miscellaneous
pydantic
